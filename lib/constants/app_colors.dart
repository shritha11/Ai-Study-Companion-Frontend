import 'package:flutter/material.dart';

class AppColors {
  // Backgrounds
  static const background = Color(0xFF0A0F1E);
  static const surface = Color(0xFF111827);
  static const surfaceElevated = Color(0xFF1A2235);
  static const card = Color(0xFF141D2E);

  // Accents
  static const primary = Color(0xFF14B8A6);      // teal
  static const primaryGlow = Color(0x3314B8A6);  // teal with opacity
  static const secondary = Color(0xFF10B981);    // emerald
  static const accent = Color(0xFF06B6D4);       // cyan

  // Text
  static const textPrimary = Color(0xFFF1F5F9);
  static const textSecondary = Color(0xFF94A3B8);
  static const textMuted = Color(0xFF475569);

  // Bubbles
  static const userBubble = Color(0xFF134E4A);   // dark teal
  static const userBubbleBorder = Color(0xFF14B8A6);
  static const aiBubble = Color(0xFF141D2E);

  // Borders / Dividers
  static const border = Color(0xFF1E293B);
  static const borderHighlight = Color(0xFF334155);

  // Status
  static const success = Color(0xFF10B981);
  static const warning = Color(0xFFF59E0B);

  // Subject colors
  static const subjectProg = Color(0xFF14B8A6);
  static const subjectMath = Color(0xFF6366F1);
  static const subjectBio = Color(0xFF10B981);
  static const subjectPhys = Color(0xFFF59E0B);
}